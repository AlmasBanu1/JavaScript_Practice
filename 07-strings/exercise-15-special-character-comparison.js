let character = '@';
if(
    !(character >= 'A' && character <= 'Z') &&
    !(character >= 'a' && character <= 'z') &&
    !(character >= '0' && character <= '9')
){
    console.log("Special Character Found");
}
 